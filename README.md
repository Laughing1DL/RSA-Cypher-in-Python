# RSA-Cypher-in-Python
A RSA Message cyhper and decyhper made in python

the .ipynb has a code that uses the libraries of 'crypto' and 'base64' but the .py file doesn't use any libraries

I doesn't use any libraries

It can be used with argparse or with inputs, that's your choice
